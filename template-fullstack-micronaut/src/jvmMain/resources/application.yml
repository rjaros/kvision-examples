micronaut:
  application:
    name: template-fullstack-micronaut
  router:
    static-resources:
      default:
        enabled: true
        mapping: "/**"
        paths:
          - classpath:public
